# 04 · 验收指引(2026-08-31 假功能下线 + 核心闭环核查)

> **这篇解决什么问题、写给谁**:给验收人(用户本人)的操作手册——怎么把系统跑起来、
> 用什么账号、逐端该看到什么/不该看到什么。本轮验收目标:**假功能全部下线、
> 三端数据互通、核心功能运行正常**。

## 一、启动(代码在 main,工作区用 `_wt/structural-debt`)

> 主工作树(`qiming/`)当前停在 `task/lab-anim-motion` 分支(动态讲义在途),
> 验收请用已装好依赖的 worktree:`/Users/apple1/Desktop/edu/_wt/structural-debt`。

```bash
cd /Users/apple1/Desktop/edu/_wt/structural-debt

# 1. 基础设施(已在跑则跳过;共用现有 qiming-postgres-1 / qiming-redis-1 容器)
docker ps | grep qiming    # 应见 postgres/redis Up

# 2. 后端(验收库 qiming_wt 已建好并 seed;.env 已指向它)
cd apps/server && npm run start          # :3000(昨晚已后台起了一个,若在跑可跳过)

# 3. 三端(各开一个终端;VITE_USE_MOCK=false 走真后端)
cd apps/admin   && VITE_USE_MOCK=false npm run dev   # :5173 管理端
cd apps/teacher && VITE_USE_MOCK=false npm run dev   # :5174 教师端
cd apps/student && VITE_USE_MOCK=false npm run dev   # :5175 学生端
```

**账号**:管理员 `13800000001 / Admin@123`;教师 `13800000002 / Teacher@123`;
学生无初始密码——在管理端学生列表点「重置密码」拿明文,再用学号(如 `S-0002`)登录学生端
(这本身就是管理端→学生端互通的第一项验收)。

**如需重置演示库**(回到干净基线;**先停后端再重置**,`DROP ... WITH (FORCE)` 会掐断连接导致后端退出):

```bash
docker exec qiming-postgres-1 psql -U qiming -d qiming_dev -c "DROP DATABASE qiming_wt WITH (FORCE)" -c "CREATE DATABASE qiming_wt OWNER qiming"
cd apps/server && npx prisma migrate deploy && npm run db:seed:base && npm run db:import-kp && npm run db:seed:business
```

## 二、验收清单 A:假功能应当「看不见」

| 端 | 检查点 | 期望 |
|---|---|---|
| 教师端 | 侧栏 | **无「实验室」分区**(ai_courseware 已 off) |
| 教师端 | 直接访问 `/courseware/new` | 提示「内测功能,请联系管理员开通」,不进向导 |
| 教师端 | 工作台 | 只有 3 张统计卡,**无「AI 预批·已开启」**写死卡 |
| 教师端 | 作业批改页 | 副标题为「解答题逐份人工批改后出分」,无 AI 预批话术 |
| 教师端 | 组卷页发布面板 | 解答题批改 Tag 为「教师批改出分」 |
| 教师端 | 学情分析 → 点待关注学生 | **无「✦ AI 诊断」按钮**(机构设置默认关) |
| 学生端 | 课程时间线/今日 | 文案「在线课堂」,**无「AI 伴学课堂」「约 100 分钟」** |
| 学生端 | 进入课堂作答 | **底部无紫色「小启」旁白条**;答对/答错正常反馈 |
| 学生端 | 课堂小结环节 | 「课堂小结」中性文案,**无「AI」徽章、无小启人设** |
| 学生端 | 作业作答(解答题) | 「由老师批改」口径;拍照上传可用(真功能保留) |
| 管理端 | 实验室管理页 | ai_courseware 与 photo_pregrade 均「未开放」(off 登记态,合法) |
| 管理端 | 平台设置 → AI 能力 | 伴学/预批/诊断三项显示**「已下线」**,仅答疑引导开关可操作 |

## 三、验收清单 B:核心闭环与三端互通(全真实数据)

推荐按这条完整业务链走一遍(约 15 分钟):

1. **管理端**:登录 → 工作台统计(教师 2/学生 12,真实聚合)→ 学生列表 → 给 `S-0002`(周子航)
   重置密码拿明文。
2. **学生端**:S-0002 登录 → 「今日」页看到真实作业任务 → 打开「第3讲课后作业」作答提交
   (客观题即时自动判分)→ **错题本**(S-0002 有 seed 错题,新答错的题会追加)→ 「报告」页周统计。
3. **教师端**:登录 → 工作台「待复核答卷」→ 批改页人工给解答题打分 → finalize 出分
   → 学情分析:课程热力图与待关注名单(真实掌握度)。
4. **课堂互通**(可选,验 WS):教师端讲次详情「发布」→ 学生端课程时间线「进入课堂」
   → 随堂作答 → 教师端课堂监控页看到实时 roster 与作答进度。
5. **AI 答疑**(真实保留项):学生课堂右侧「AI 助教」提问——**若 `.env` 配了真实 `LLM_API_KEY`**
   是真模型回复;未配 key 时走 mock 路由,回复带「mock 引导回复」字样(部署上线必须配 key,
   或在管理端「AI 接口管理」明确切 mock)。

有 seed 错题的学生(供错题本演示):S-0002 周子航、S-0003 吴佳怡、S-0006 王浩然、
S-0010 孙铭、S-0011 黄子睿、S-0012 李一诺(各 1 条)。

## 四、本轮核查已跑过的机器验证(supporting evidence)

- 鲜库(重建 + 全量 seed)后端全量 e2e:**30 套件 / 334 用例,连续三轮全绿**(含课堂 WS、
  批改闭环、租户隔离、功能开关默认态)。
- `check:all` 全绿:契约三连(84 paths)+ ui 84 + admin 67 / teacher 284 / student 147 +
  三端 build + lab build + server build;三端 mock 冒烟全部通过。
- 真实栈 API 冒烟:三角色登录、admin 发学生密码→学生登录闭环、`/features/my` 为空、
  诊断端点 403、聚合端点真实数据。
- CI(GitHub Actions)在推送后自动复跑 checks + e2e。

## 五、已知边界(验收时不要误判为 bug)

- **AI 答疑无 key 时回复带 mock 字样**——按设计的路由态,不是假功能残留;配 key 即真实。
- 批改复核页若出现「预批均分(历史)」/历史 aiScore,是下线前的真实历史数据,如实展示。
- 资源库里若有带「AI 生成」标签的历史课件,是真实存在的资源文件,可手动删除;它们**不会**再进课堂。
- 教师监控页需真实 WS(`VITE_USE_MOCK=false` 启动即真实源)。
- 学生端登录后若提示休息时段,是 org 学习时段门禁(seed 已放宽为全天,正常不会触发)。

*生成日期:2026-08-31。验收发现问题记录到本文件末尾或直接反馈。*
