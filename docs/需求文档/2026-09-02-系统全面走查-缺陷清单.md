# 系统全面走查 · 缺陷清单(2026-09-02)

> **这篇解决什么问题、写给谁**:2026-09-02 对三端 + 四条闭环在真实模式(真后端 + 鲜库 `qiming_walk` + Playwright Chromium)
> 做了一次全面走查,本文是**唯一的结果汇总**:按根因分组的缺陷、分级、修复归属、需要用户拍板的口径。
> 修复波次以本文为需求来源;每修一条在「状态」列回销。
>
> 方法与范围见 `2026-09-02-系统全面走查-计划.md`;逐条证据(复现步骤、截图、API 原文)在工作区外
> `_lab/ui-walkthrough/findings/W0–W4.md` 与 `shots/`;走查脚本 `_lab/ui-walkthrough/W1–W4/*.mjs` 可重跑复查。

## 〇、修复状态(2026-09-02 晚)

**P1 12 项全部已修,P2 15 项全部已修(C-4 拍板删掉 AI 文案),P3 按表处理**;复查(重跑走查脚本 + 新增 `W4/classroom-ui.mjs` / `W4/courseware-ui.mjs` 全 UI 课堂链)又发现并修掉 2 项:互动课件 HTML 直链为 octet-stream 导致课堂 iframe 只弹下载(改为内联 + CSP sandbox)、无小结段的讲次下课后学生停在随堂练(下课一律进小结);修复分 5 支合入:`task/walk-server` → `task/walk-contract`(经批准)→ `task/walk-teacher` / `task/walk-student` / `task/walk-admin` → seed。回归:鲜库全量 e2e 32 套件 / 347 用例、`check:all`、三端 mock 冒烟全绿;走查脚本复查见「第六节」。

## 一、结论一句话

**核心闭环(建号 → 录题组卷 → 编排发布 → 学生作答 → 批改出分 → 成绩单/错题本/报告 → 学情;课堂加入/作答/AI 答疑/控场/下课/入账)全部真实跑通。**
问题集中在四类:① 分类结构缺失(知识点/学科/资源没有贯通),② 课堂链在 UI 层有断点(教师端无下课按钮、课件不下发),
③ 8-31 下线后残留的假 AI 文案与不一致口径,④ 两个安全项(停用不即时、时区)。共 **P1 12 · P2 15 · P3 20**,另 6 项待产品拍板。

## 二、用户提出的 5 个问题

| # | 现象 | 结论 | 去向 |
|---|---|---|---|
| 1 | 教师重置密码「复制密码」出错 | **未复现**。localhost 剪贴板内容 = 密码;LAN(非安全上下文)走 `execCommand` 降级并提示已复制。07-07 修复 `d67592b` 已覆盖 | 请在 Safari/iPad 真机点一次;若失败,已知代码路径(`admin/lib/copyText.ts`:`await` 后的 `execCommand` 在 Safari 可能脱离手势) |
| 2 | 知识点库缺「科目」选择,只见化学 | **坐实**:`pickKnowledgeGraph` 取第一个教材图谱,化学恰为 id 1;页面无学科切换 | A-1 |
| 3 | 挂载课件内容重复列出 | **未复现**(三处面板含置顶分支均无重复,条数 = API) | 请回忆当时是否 mock 模式 / 资源是否 >50 条;资源多时复验 |
| 4 | 试卷缺「分类」(学科 / 知识点) | **坐实**:`PaperDto` 无 subject / kpNodeIds;试卷库与三处选卷面板无筛选 | A-2(契约变更) |
| 5 | 错题本没有显示错题 | **坐实为产品口径**:含主观题的作业交卷后,答错的客观题要等教师 finalize 才入错题本;纯客观卷即时入 | F-1 待拍板 |

## 三、缺陷清单(按根因分组)

级别:**P0** 闭环阻断 · **P1** 功能错误/安全 · **P2** 体验/一致性 · **P3** 小项。归属:契约 / 后端 / 前端(端) / 产品 / 部署 / seed。

### A · 分类结构缺失(同根:知识点—学科—资源—试卷没有贯通)

| ID | 级别 | 现象 | 根因 | 归属 | 状态 |
|---|---|---|---|---|---|
| A-1 | P1 | 知识点内容库只显示化学图谱,无学科切换;搜不到时空态说「图谱未加载」 | `teacher/pages/knowledge/lib/graph.ts` 取第一个 curriculum 图谱;题库页已有 `bank/lib/kpTree.ts` 的按学科解析可复用 | 前端(teacher) | 已修 · teacher |
| A-2 | P1 | 试卷无学科 / 知识点:试卷库只有类型 Tab + 名称搜索;编排「选择试卷/选择已有卷」、组卷选卷面板全混列 | `PaperDto` 缺 `subject`、`kpNodeIds`(可由题目学科与标注聚合推导,无需迁移) | **契约** → 后端聚合 → 前端筛选 | 已修 · 契约+server+teacher |
| A-3 | P1 | 资源无法在 UI 挂知识点:上传不问、无编辑、列表无知识点列;编排「本知识点」置顶与知识点库「关联资源」永远空 | 契约与服务端已支持 `kpNodeId`(POST/PUT),仅资源库页缺入口 | 前端(teacher) | 已修 · teacher |
| A-4 | P2 | 错题本错因分组只剩「全部」 | 分组用预批的 `aiErrorTags`,预批已下线;应回退题目知识点/能力标签 | 后端 + 前端(student) | 已修 · server |
| A-5 | P3 | 三维标注弹窗默认页签「解题能力」,必填的却是「教材知识点」;搜索只在当前页签内 | 前端默认值 | 前端(teacher) | 已修 · teacher |
### B · 课堂链的 UI 断点(服务端与协议齐全,前端没接)

| ID | 级别 | 现象 | 根因 | 归属 | 状态 |
|---|---|---|---|---|---|
| B-1 | P1 | 教师端课堂监控页**没有下课 / 暂停 / 切环节按钮**;`class:control` 在 UI 上无法发出 | `MonitorPage` 只监听 `monitor:*`;协议、服务端、e2e a6 都有 | 前端(teacher) | 已修 · teacher |
| B-2 | P1 | 学生课堂讲解环节永远「课件暂不可用 · 老师还没有上传本节课件」 | `classroom.service#buildCourseware` 只组装结构化逐页对象(`config.pages`/`meta.pages` 含 title/body),没有 UI 能产生;PDF/PPT/视频/图片/互动一律不下发 | 后端 + 前端(student) + 产品(支持哪些类型) | 已修 · 契约+server+student |
| B-3 | P2 | 「选择已有卷」挂的课后作业只在下课时推送;不开在线课堂就永不推送;「去组卷」却是立即布置。两处文案都写「下课推送」 | 设计如此(`classroom.service.ts:886`),但缺立即布置入口与准确文案 | 产品 → 前端(teacher) | 已修 · 拍板「立即布置」:编排页加按钮(已布置态),两处文案改准;下课自动推送幂等保留 |
| B-4 | P3 | 下课后监控页仍显示「1/1 人在堂」,无「已结束」态 | 前端未处理 end | 前端(teacher) | 已修 · teacher |
| B-5 | ? | 仅有作业环节的直播会话(第 5 讲)监控页一直骨架屏;有讲解/随堂练环节的会话正常 | 待复验(可能是源判定条件) | 前端(teacher) | 已修 · teacher(join 快照即视为已连上) |
| B-6 | P3 | 讲次列表「已就绪 ✓」与「备课进度 1/2 · 课后作业 ✕」并列 | 放宽规则后文案没跟 | 前端(teacher) | 已修 · teacher |
### C · 假 AI / 过时文案残留(违反 2026-08-31「只保留真实闭环」)

| ID | 级别 | 现象 | 归属 | 状态 |
|---|---|---|---|---|
| C-1 | P1 | 编排页每单元「小结巩固:AI 按每位学生本单元错题生成 2–4 道巩固题 [AI]」+ 编排说明;知识点库内容包「小结模板 [AI] · 题量 2–4 道」——服务端**没有任何生成巩固题的实现** | 前端(teacher)+ 产品(巩固题是否列二期) | 已修 · teacher |
| C-2 | P2 | 复核页副标题「公式填空 AI 预批参考」、名单「AI 预批 —」;录题页「参考答案 · AI 预批将按要点逐步给分」 | 前端(teacher) | 已修 · teacher |
| C-3 | P2 | 管理端「AI 接口管理」把已下线的预批 / 伴学 / 诊断 / 课件四项显示为「真实」且可切换,与平台设置「已下线」、实验室「未开放」口径打架;env 感知默认全 real,重启即烧钱 | 前端(admin)+ 后端默认路由 | 已修 · admin+server 默认路由 |
| C-4 | P3 | 学生端登录页「AI 帮学生学透每道题 · 由 AI 带着完成闭环」 | 产品文案 | 已修 · 拍板删掉:改为「学生在平板上学透每道题 / 课堂练习即时判分,错题自动收进错题本,学习报告按知识点看掌握情况」 |

### D · 安全与权限

| ID | 级别 | 现象 | 根因 | 归属 | 状态 |
|---|---|---|---|---|---|
| D-1 | P1 | **停用账号不即时生效**:教师/学生被停用后旧 access token 继续可用到过期(2h) | `JwtAuthGuard` 不校验 status;停用不写吊销位(改密/重置的 `auth:pwdreset` epoch 机制可复用) | 后端 | 已修 · server |
| D-2 | P1 | **学生端使用时段按服务器进程时区判断**;生产镜像未设 `TZ`(UTC),「06:00–22:30」会错位 8 小时 | `common/student-hours.ts` 用 `now.getHours()`;Dockerfile/compose 无 TZ | 后端 + 部署 | 已修 · server+Dockerfile |
| D-3 | P2 | 同机构他教师可读他人课程的讲次编排、讲次列表、**学情掌握度**(200),编排页还渲染可编辑按钮(写才 404);批改页 404 被解释成「网络波动」 | 读侧无归属校验;前端无只读态与准确错误文案 | 产品 → 后端 + 前端(teacher) | 已修 · 拍板「可读」:读侧保持;编排页对非归属教师只读(隐藏保存/发布,标「他人课程 · 只读」);批改页 404/403 给「无权批改或作业不存在」 |
| D-4 | P2 | 教师停用后登录提示「账号或密码错误」(学生是「已停用」) | auth.service 教师分支未区分 | 后端 | 已修 · server |
| D-5 | P3 | 密码吊销同秒边界:同一秒内签发的 token 不被吊销(`iat < epoch`) | 后端 | 接受(严格小于是为了不误杀「重置后立刻登录」的新 token;边界已在 pwd-reset.ts 注明) |
| D-6 | P2 | 班型 one_on_one / one_on_three 不限制人数(API 200 加第二人) | 产品口径 → 后端校验 | 已修 · 拍板「统一 ≤100 人」:创建/加人/同步名单超出 409 |

### E · 空值 / 空态 / 错误态呈现

| ID | 级别 | 现象 | 归属 | 状态 |
|---|---|---|---|---|
| E-1 | P1 | 复核页名单显示「林小满 · **null 分** 已复核」×9、头部「12 份待判分…剩余 0 份」与全部已复核矛盾、作答照片破图 | 前端(teacher)(空值渲染)+ seed | 已修 · teacher |
| E-2 | P2 | 无课教师的「我的课程」「学情分析」页只有标题,没有空态(工作台有好文案可复用) | 前端(teacher) | 已修 · teacher |
| E-3 | P2 | 管理端工作台「最近动态」:React 重复 key(`王校长 · auth.login`);文案是原始动作码 `auth.login` / `admin.course.remove_student`(**服务端拼的**) | 前端(admin)+ 后端 dashboard | 已修 · server+admin |
| E-4 | P2 | 学生时间线「N 道错题待订正」的「订正错题」按钮永不出现:依赖 `kind=correction` 作业,服务端无处创建 | 产品(correction 是否保留)→ 前端(student) | 已修 · 拍板「保留」:出分后按答错客观题自动生成订正作业(幂等);无订正作业时文案改「可在错题本回顾」 |
| E-5 | P3 | 管理端「本周到课率 —」无解释(无课堂记录时应说明);课堂跑过一次后变 100% 正常 | 前端(admin) | 已修 · admin |
| E-6 | P3 | 编排页「保存编排」成功无 toast;`/grading/*` 面包屑显示「我的课程」 | 前端(teacher) | 已修 · teacher |
### F · 产品口径待拍板(先定再修)

| ID | 问题 | 建议 |
|---|---|---|
| F-1 | **错题入账时机**(#5):含主观题的作业,客观错题是否在交卷时即时入错题本? | 建议即时入(客观题已判定),主观题出分后补;成绩单/错题本显式标「待出分」 |
| F-2 | **课后作业推送**:「选择已有卷」挂卷是否要提供「立即布置」?两条路径是否统一? | 建议编排页加「立即布置」,并把两处文案改准 |
| F-3 | **同机构教师互读**:讲次编排、学情是否允许非归属教师读? | 建议学情(学生数据)限归属教师 + 管理员;编排页对非归属教师只读或 404 |
| F-4 | **班型人数**:one_on_one / one_on_three 是否硬限制? | 建议后端校验 + 409 提示 |
| F-5 | **correction 作业**:「订正错题」是否保留?若不,时间线改为链到错题本 | 建议删死代码,统一走错题本重做 |
| F-6 | **试卷草稿态**:创建即 published,编排页「未发布卷」提示永不触发;是否需要草稿? | 建议保留现状、删除死提示,或加「保存草稿」 |
| F-7 | 使用时段外**已登录**学生是否踢出? | 文案说「无法登录」,现状一致;按管控需求定 |

**拍板结果(2026-09-02,用户)**:F-1 是(客观题交卷即入账)· F-2 立即布置 · F-3 可以(同机构可读,前端只读态)· F-4 限 100 人 · F-5 保留(自动生成订正作业)· F-6 要草稿态 · F-7 不踢出。全部已落地,见各行状态。

### G · 环境 / 部署 / seed

| ID | 级别 | 现象 | 归属 | 状态 |
|---|---|---|---|---|
| G-1 | P2 | `npm run start:dev` 起不来(nest watch 找 `dist/main`,产物在 `dist/apps/server/src/main.js`),而 README / 架构总览推荐它 | 环境(nest-cli entryFile)+ 文档 | 已修 · server 脚本 |
| G-2 | P2 | 回看直链 `GET /storage/*` 恒返 `application/octet-stream`,新窗口预览 PNG/PDF 变下载 | 后端(storage 按扩展名设 Content-Type) | 已修 · server |
| G-3 | P3 | seed:两个资源 ossKey 指向不存在文件(预览 404);1v1 课程 total_lessons=16 但 0 讲次;第 3 讲作业 dueAt 早于 publishAt;第 4 讲三单元无知识点;S-0002 attempt.score=24 ≠ answers 和 15;错题解析文案与题不符;作答照片 key 无文件 | seed | 已修 · seed(实体文件 / 15+16 讲 / 单元知识点 / 时间 / 分数一致 / 解析) |
| G-4 | P3 | 本机 Postgres 堆了 6 个历史库(`qiming_classroom/cw/dev/regress/verify/wt` + 本轮 `walk`) | 环境 | 待清理(复查后) |

### H · 其他体验小项

| ID | 级别 | 现象 | 归属 | 状态 |
|---|---|---|---|---|
| H-1 | P2 | 学生学号登录不 trim / 不归一大小写(「 s-0002 」401) | 后端 | 已修 · server(登录归一 + 自定义学号落库大写) |
| H-2 | P3 | 新建课程默认预选第一位在职教师(有「请选择」却不作默认);新建教师默认「初中 · 数学」 | 前端(admin) | 已修(课程教师必选);教师表单默认学科保留 |
| H-3 | P3 | 「去组卷 · 发布作业」页文案「下课后推送」与立即可见的行为不符 | 前端(teacher) | 已修 |
| H-4 | P3 | 手机竖屏(390px)导航触控目标 26px、任务卡按钮 24px(< 44);设计目标是平板,备查 | 前端(student) | 备查 |
| H-5 | P3 | 新建试卷默认类型 homework 与名字无关 | 前端(teacher) | 不改(类型由教师选择) |
| H-6 | P1 | **管理端到课率 / 作业完成率把 0–1 比值直接拼 %**(100% 显示成「1%」;mock 数据恰为 94.2 掩盖)——修复时新发现 | 前端(admin) | 已修 · admin(formatRate 统一,mock 改比值) |

## 四、修复建议顺序(先定 F,再按分支)

1. **拍板 F-1 ~ F-7**(半小时)。
2. `task/walk-contract`:A-2(Paper 加 subject / kpNodeIds)——申请 → 批准 → gen:sdk → 先合。
3. `task/walk-server`:D-1、D-2(+ Dockerfile `TZ`)、D-4、D-5、A-4 后端侧、E-3 后端侧、G-2、H-1、B-2 后端侧、(F-3/F-4 如批准);补 e2e:停用即时 401、时段用固定时区、班型人数、非归属读侧。
4. `task/walk-teacher`:A-1、A-3、A-5、B-1(控场四按钮)、B-4、B-6、C-1、C-2、E-1、E-2、E-6、H-3、H-5、B-2 前端侧;vitest 覆盖空值渲染与知识点解析。
5. `task/walk-student`:B-2 渲染、E-4、A-4 分组、C-4。
6. `task/walk-admin`:C-3、E-3、E-5、H-2。
7. `task/walk-seed`:G-1、G-3;更新 `docs/01` 启动命令与 `docs/04` 验收指引(仍指向已删的 `_wt/`)。
8. 复查:重跑 `_lab/ui-walkthrough/W1–W4` 脚本 + `check:all` + e2e;把值得留的 UI 断言迁进仓库(是否进 CI 另议)。

## 五、走查覆盖与未覆盖

覆盖:三端全部路由(admin 11 / teacher 21 / student 10)、四条闭环、E1 功能分级、使用时段门禁、LAN 非安全上下文、Chromium 桌面 + 平板 + 手机三视口。
未覆盖:多机构隔离(单机构 seed,e2e 有覆盖)、OSS 驱动、真实生图(功能 off)、Safari / iPad 真机、公式键盘实际 LaTeX 输入、上传 PDF/PPT 的类型识别、课堂 WS 断网重连(只测了刷新)。

*生成日期:2026-09-02。基于 main `b5235ec` + 库 `qiming_walk`。*
